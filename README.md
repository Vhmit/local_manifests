Moto Legacy
===========

Getting started
---------------

To initialize your local repository using the derp13 trees, use a command like this:
```
git clone https://github.com/Vhmit/local_manifests --depth 1 -b derp13 .repo/local_manifests
```
